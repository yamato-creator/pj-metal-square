# デプロイ状況（2026-06-17 更新）

## 本番環境

| 環境 | URL | 状態 |
|------|-----|------|
| フロント (Vercel・旧) | https://www.preciousmetalmine.com | ❌ **7ヶ月前のビルド**（DNSは旧プロジェクト向き） |
| **フロント (Vercel・新)** | **https://pj-metal-square-three.vercel.app** | ✅ **最新コード稼働中**（2026-06-17 デプロイ） |
| バック (Render) | https://api.preciousmetalmine.com | ✅ 最新コード稼働中 |

## 2026-06-17 アクション

旧 Vercel プロジェクト（team `team_B1bKeLaghUy11hUisTmmDkF2` = `pj-metals-projects`）に
小倉のアカウント（`tool-8239` / `dydydydydydy5656-gmailcoms-projects`）でアクセスできなかったため、
**自分のVercelチームで新規プロジェクトを作成して最新コードをデプロイした**。

- 新Production URL: https://pj-metal-square-three.vercel.app
- バンドル: REACT_APP_API_URL=https://api.preciousmetalmine.com で埋め込み済み
- 検証済: sessionStorage / authStorage コードが本番JSに含まれていることを確認

### DNS 切替（次のアクション）

`www.preciousmetalmine.com` の Vercel プロジェクトを新側に向ける手順:

1. 旧 Vercel チーム（pj-metals-projects）にログインして `www.preciousmetalmine.com` のドメインを Remove
2. 新 Vercel プロジェクト（`dydydydydydy5656-gmailcoms-projects/pj-metal-square`）の Settings → Domains で `www.preciousmetalmine.com` を Add
3. DNS（Cloudflare / Route53 等）の CNAME を新Vercelが指示する値に更新（伝播 ~5分）
4. 反映後、 https://www.preciousmetalmine.com で新コードが表示される

## Vercelデプロイ失敗の経緯

- **最後の成功**: 2025/9/15（commit `f9245e2`）
- **その後**: コミット push するたびに Vercel がデプロイを試みるが、全て **0-1秒で即失敗**
- 2026-03-07 / 2026-03-18 / 2026-04-20 複数回の push 全部同じパターン

### 即失敗の意味

Vercel がビルドを**開始すらせず**rejectしている = プロジェクト設定レベルの問題:
- プラン（Free tier）の制限超過の可能性
- GitHub integration の接続切れ
- 支払い関連のブロック
- プロジェクトが paused / archived
- ブランチ保護ルールの設定

### ビルド自体は正常

ローカルビルド確認済み:
```bash
CI=false npm run build  # ← 成功（warning のみ）
```

## 小倉さんのアクション必要

1. https://vercel.com/pj-metals-projects/pj-metal-square/deployments を開く
2. 最新の失敗デプロイをクリックしてログを確認
3. 「Source」「Build Logs」「Runtime Logs」タブで原因特定
4. 可能なら `ogura.yamato123@gmail.com` または Vercel CLI の `tool-8239` をプロジェクトに招待
   → 私が直接ログ見て対処可能

## 応急対応（フロント/バック不整合）

現在、バック側だけ新コード稼働しているため、
旧UIで「売却手続きへ」をユーザーがタップすると:
- 旧フロント: 「売却完了」動作を期待
- 新バック: 「見積依頼」として処理し、資産を減らさない
- メール: 見積依頼受付メールが送信される

ユーザー体験は混乱するが**実害なし**（資産減らず、管理者に依頼が届く）。

## 今回追加された変更（全て main に push 済み）

| Commit | 内容 |
|--------|------|
| `80bfa62` | ⑩ 売却→見積もり依頼フロー（フロント+バック+メール） |
| `14dc5ae` | ⑨ プライバシーポリシーページ |
| `cbdc588` | ⑦ スプシからの売却入力GAS |
| `c3e906b` | ⑩ 単体テスト（backend 8 + frontend 5） |
| `8cfb48d` | fix: Vercel CI=false + ESLint bypass |

## 関連ドキュメント

- `gas/README_売却入力.md` - ⑦ スプシ売却入力の運用手順
- `setup_sale_input_sheet.py` - 売却入力シートの初期セットアップスクリプト
